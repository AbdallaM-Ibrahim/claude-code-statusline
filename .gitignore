# Build artifacts — the installed binary lives at ~/.claude/statusline.exe.
# Anchored to the root so they do not swallow cmd/statusline/.
/statusline
/statusline.exe
dist/

# Scratch pricing experiments. The real override is a pricing.json placed next to
# the installed binary — nothing reads this name.
pricing.local.json

# Benchmark output
bench/results*.json
